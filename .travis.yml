dist: xenial
sudo: required
group: bluezone
language: generic

env:
  global:
    - COMPONENT_AUTO_COMMAND=${TRAVIS_BUILD_DIR}/chartAutomation.sh


branches:
  only:
    - trigger-3569

  # The /^ga-[0-9]+.*$/ pattern is for tag testing only, so it can be removed when no one needs to test tagging against commits.

stages:
  - auto

jobs:  
  include:
    - stage: auto
      name: "Auto trigger mch-repo"
      script: 
        - make component/auto
